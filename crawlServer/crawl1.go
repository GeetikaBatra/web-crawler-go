import(
   "fmt"
   // "github.com/streadway/amqp"
)

// func crawl(url string) []string{
// 	fmt.Println(url)
// 	list, err := links.Extract(url)

// 	if err != null
// }

func main() {
	// conn, err := amqp.Dial("amqp://guest:guest@crawl-broker:5672/")

	// failOnError(err, "Failed to connect to RabbitMQ")

	// defer conn.Close()
	fmt.Prntln("*******************	working")
	for {}

}